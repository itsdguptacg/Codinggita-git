We use git clone for cloning any GitHub repo and we use git init to make any local directory A local repository.
Git status tells me about Which files are untracked modified staged and what are the changes I have left to be committed in local repo Also it can the branch name you are currently on.
Git add is used to Move any or folders wrong local directory to staged area.
git diff Changes between what are on the staged the area and What are in directory and git diff --staged Tells about the changes between files changes and files in the staged area
It is used to commit any changes to local repo
Git log shows full detail of any commit vs Git oneline only commit ID and commit messageOnly get ID and commit message.
It shows information about commits for example username and Many more things
It pull data from the repository of any remote repo like get up to the local Repo
It pushes any commit from the local Repo to the remote repo hlike github
It is a command used in Git to make Any developer to work on another branch of the repo
It is an older way to switch branches in repo using git
To first make a stable code which can be able to be committed very goodly or you could say very safely to the main branch which is deployed or something so I dosent break.
Pull request is a request to merge any code from the branch to the main branch
It is important check full request to be reviewed before merging to be able to ensure that the good for the main branch otherwise it could destroy any project. 
May is the main branch and feature pages is the Branch of that which has a of main.